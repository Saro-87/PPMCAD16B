---
all:
  vars:
    ansible_ssh_private_key_file: /root/.ssh/id_rsa
    ansible_user: root

  children:
    web:
      hosts:
        web1:
          ansible_host: 13.216.241.19  # Replace with the actual IP

      vars:
        java_package_name: "openjdk-25-jdk"
        java_home: "/usr/lib/jvm/java-25-openjdk-amd64/"